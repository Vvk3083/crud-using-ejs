<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-full bg-zinc-800 p-10 text-white">
        <div class="nav mb-10">
            <a class="text-blue-500" href="/read">Read</a>
        </div>
        <h1 class="px-5 text-3xl tracking-tighter mb-3">Update user</h1>
        <form action="/update/<%= user._id %>" method="post">
            <input class="px-5 py-2 border-2 border-zinc-800 bg-transparent rounded-lg ouline-none" type="text" name="name" placeholder="enter name" value="<%= user.name %>">
            <input class="px-5 py-2 border-2 border-zinc-800 bg-transparent rounded-lg ouline-none" type="text" name="email" placeholder="enter email" value="<%= user.email %>">
            <input class="px-5 py-2 border-2 border-zinc-800 bg-transparent rounded-lg ouline-none" type="text" name="image" placeholder="enter image url" value="<%= user.image %>">
            <input class="px-5 py-2 border-2 bg-yellow-500 border-zinc-800 rounded-lg ouline-none" type="submit" value="Update User">
        </form>
    </div>
    
</body>
</html>